# Core dependencies
setuptools==65.5.0   # Stable version for building packages
joblib==1.4.2         # Required for model saving/loading
numpy==1.23.5         # Compatible version of NumPy
pandas==2.2.3         # Ensure compatibility with pandas version
scikit-learn==1.0.2   # Working version for sklearn

# Additional dependencies (if needed)
matplotlib==3.7.0     # For plotting in Streamlit
scipy==1.9.3          # Required for scientific computations
tensorflow==2.10.0    # For LSTM models, if needed
streamlit==1.17.0     # For running the Streamlit app
joblib==1.2.0         # Ensure joblib compatibility

